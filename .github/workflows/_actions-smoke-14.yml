name: Actions Smoke 14

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-22.04
    steps:
      - name: Smoke
        run: |
          set -e
          echo "ACTIONS_SMOKE_OK"
          uname -a
